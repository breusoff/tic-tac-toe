import React, {FC} from "react";
import {SelectWrapper} from "./Select.styles";

interface ISelectProps {}

const Select: FC<ISelectProps> = () => {
    return <SelectWrapper>Select</SelectWrapper>;
};

export default Select;
